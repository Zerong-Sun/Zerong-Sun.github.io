---
import BaseLayout from '../../layouts/BaseLayout.astro';
import NoteCard from '../../components/NoteCard.astro';
import TagList from '../../components/TagList.astro';
import { getAllNotes, getAllTags } from '../../lib/notes.ts';

const notes = await getAllNotes();
const tags = await getAllTags();
---
<BaseLayout title="Notes">
  <div class="mx-auto max-w-content px-4 py-10 md:px-6">
    <h1 class="mb-4 text-3xl font-bold text-[var(--text)]">Notes</h1>
    <p class="mb-6 text-[var(--text-secondary)]">{notes.length} essays</p>
    <div class="mb-8">
      <TagList tags={tags} />
    </div>
    <div class="grid gap-5">
      {notes.map((note) => <NoteCard note={note} />)}
    </div>
  </div>
</BaseLayout>
